let array = [];
let n = 0;
let m = 100;
let count = 100;
let range = Math (n-m);
let min = Math (n, m);

while (array.length < count)
{
array.push (Math.floor(Math.random() * range + min))
};

console.log (array);

